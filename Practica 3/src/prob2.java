/*class MiHilo extends Thread {
    private final Matriz A;
    private Matriz C;
    int rStart, rFinish;

    public MyThread(Matriz Abar, Matriz Cbar, int rStart, int rFinish) {
        this.A = Abar;
        this.C = Cbar;
        this.rStart = rStart;
        this.rFinish = rFinish;
    }

    public void run() {// write code hereC.setMatrix(matC);

        @Override
        public void run () {
        }
    }

    class Matriz {
        public Matriz(Integer d) {
            dimension = d;// constructor code
             }public static Matriz SimpleFilter(Matriz A ) {} public static Matriz Filter(Matriz A, int numHilos) throws InterruptedException{}// member for get & set of elements // member for printing }class prob2 {public static void main(String[] args) {// set dimension of Matrix// set number of threads// Generate Matrix// call the SimpleFilter // or call the Threaded Filter}

             */
